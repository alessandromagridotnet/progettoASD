package ReteAutomi;

public class PseudoCodice {
    /*
    Else if (confermato==1)
        {
            ArrayList<String> rilevanzaFinale = new ArrayList ();
            finale= t.getIniziale.getRilevanza();
            finale.pushRilevanza(t.getRilevanza());
            if !(finale.notEqual(successivo.getRilevanza())
                {
                    successivo.clone ();
                    successivo.setRilevanza (finale);
                    PIEDE RICORSIONE (etichettaturaRilevanza(successivo)) ????????
                }
            else if (finale.equal(successivo.getRilevanza())
            {
                DOBBIAMO DAVVERO VALUTARE QUESTO CASO? ALLA FINE SE LO STATO è GIà STATO VISITATO CON LE STESSE ETICHETTE DI RILEVANZA CI POSSIAMO FERMARE NO?
                LA TRANSIZIONE CHE PORTA A QUELLO STATO E QUELLO STATO STESSO GIà ESISTONO... MA FORSE HO CAPITO MALE IO.
            }
        }
     */
}
